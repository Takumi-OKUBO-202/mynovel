# 引き継ぎメモ(セッション間の申し送り事項)

**このファイルの目的**: 別セッションに作業を引き継ぐための現状共有。恒久的な設定資料は`bible/`・`plot/`に、このファイルはあくまで「今どこまで進んでいて、次に何をやるか」の一時的なスナップショット。**詳細情報は極力重複させず、正となるファイルへのポインタに留める。** 内容が古くなったら随時上書き・削除してよい。

## 現状

- 本文確定稿: **第1話〜第25話**(`manuscript/part01/ep001.md`〜`ep025.md`)。`manuscript/_draft/`は空
- アーク1(誰そ彼、彼は誰)は第14話で**完結**
- **アーク2「露見(仮)」は第25話で完結**(第15〜25話)
- アーク3「凪と嵐(仮)」は全15話(第26〜40話)のプロットが**`plot/arcs/arc03-nagi.md`に確定済み・本文未執筆**。新キャラ4名(陽菜・拓真・ドレイク・彦次郎)のbible作成済み(`bible/characters/hina.md`・`takuma.md`・`drake.md`・`hikojirou.md`)。**詳細・伏線対応表はすべて`plot/arcs/arc03-nagi.md`に集約済みのため、このファイルには要約を置かない**

## 次にやること

**アーク3の第26話「覚醒者」を執筆する。** 内容・厳守事項は`plot/arcs/arc03-nagi.md`の第26話の節を参照(このファイルには重複させない)。

- `new-episode`スキルの手順(episode-drafter→continuity-editor→proofreader)に従う
- **アーク3全体の執筆時の注意は`plot/arcs/arc03-nagi.md`「執筆時の注意」の節に集約済み。** アーク3に着手する前に必ず一読すること

### アーク3で特に忘れやすい点(ポインタのみ、詳細はarc03-nagi.md)

- **氷牙の白虎の正体は、ドレイク・彦次郎には最後までバレない**(第38話の舞台を本隊から分ける)
- **冬夜たちは主戦力ではない。** 刻紋種を倒す決定打はドレイク・彦次郎に譲る
- **「何か」の単語は本文に出さない**(アーク2から継続)。夜会・浩二・要もこのアークでは動かさない
- **サービス開始から一年半ほど**という第2話の前提と、ドレイクの実プレイ期間が矛盾しないこと(**改訂済み。旧設定の「半年」は使わない。**正は`bible/world/game-system.md`「サービス開始からの経過期間」の節)。彦次郎と大鮫の因縁(数年前)はNPC個人史として別枠で処理する
- **第25話でマレイアの刻紋種を「名前も危険度も不明」として先行言及済み。** 第30話でマレイアに着く際は、この状態からの続きとして書くこと

## モデルとエージェント運用(確定・重要)

- **下書き作成・整合性チェックはOpusで行う。** `.claude/agents/episode-drafter.md`・`continuity-editor.md`のfrontmatterで`model: opus`を指定済みのため、**Agent呼び出し時にmodelを上書きしない**(指定なしで呼べばOpusになる)。`proofreader`以下の他エージェントはSonnetのまま
- **経緯**: 一時期Opusを指定していたが**単調・冗長**になりがちで一度Sonnetに戻した。しかしSonnetでは逆に**痩せる**問題が出た(第24話の初稿が2,272字)ため、Opusに戻した
- **これは条件付きの決定である。** Opusの単調・冗長を潰すためのプロンプト側の手当て——(1)文字数の実測を義務化、(2)「削るもの/削ってはいけないもの」を対で明記、(3)場面構成と前話との差分をメタコメントに自己申告させる——を`episode-drafter.md`に入れたうえで戻している。**この手当てを外すと同じ失敗に戻るため、セットで維持すること**

### チェックを自分で必ず走らせること(第24〜25話で実際に問題が起きた)

- **エージェントの自己申告する文字数を鵜呑みにしない。** 第24話では「約4,500字」と申告されたが実測3,144字だった(1,350字の乖離)
- **段落チェックのスクリプトに注意。** `p.strip().startswith('　')`と書くと、`strip()`が判定対象の全角スペースを除去してしまい**何も検査されない**。第24〜25話で実際にこの取りこぼしが起きた。下記の正しい版を使うこと
- **`continuity-editor`には「話数レンジの照合結果を報告に含めること」を毎回明示的に指示する。** これで第24話の致命的な矛盾(クレストの排他制御違反)が見つかった

```bash
# 文字数と4文以上の段落を同時にチェック
python3 -c "
import re, sys
t = open(sys.argv[1]).read()
b = re.sub(r'<!--.*?-->', '', t, flags=re.S)
b = re.sub(r'\`\`\`.*?\`\`\`', '', b, flags=re.S)
hits = [i for i, l in enumerate(t.split('\n'), 1) if l.startswith('　') and l.count('。') >= 4]
print('文字数', len(b), '| 4文以上の段落', hits if hits else 'なし')
" manuscript/_draft/epXXX.md
```

## 第26話開始時点のステータス(詳細は`bible/characters/touya.md`変遷ログ参照)

- 冬夜: **Lv11(第一段階)**、STR38/AGI183/DEX160/INT17/VIT9/LUK40、HP132/132・MP110/110・魂力(SP)108/108。自由配分は処理済み(第25話でAGIに全振り)
- 所持金1700Y、ギルドランクE。所持品: 傷薬(小)×4、**目眩し玉×0**(第24話で使い切り、補充が必要)、**刻紋銘装備「苔纏いの手甲」(佳品Cランク、片手分、性能未確認・未装備)**、当て革、森閉ざしの外套(刻紋銘・逸品B相当)、軽装の上着、短剣二本・投擲ナイフ
- 茜音: **第25話で復帰済み。** 不在中のことは本人も語らず、冬夜も訊いていない。**復帰後も旅行の中身を描写しないこと**(`bible/characters/akane.md`「復帰後の運用方針」)
- **確定: 冬夜の単独行動を、二人の間の摩擦として扱わない。** 第25話の路地の場面で、茜音は咎めず、約束も取り付けず、黙って聞くだけ。単独行動を縛る約束は**存在しない**。かつて「なんで、一人で行ったの」から「次は、待って。わたしが着くまで待って」までの咎め・約束のやり取りを書いていたが、**摩擦が生まれるような事柄ではないと判断して削除済み**。以後の話数で復活させないこと
- **あわせて、「茜音を守るために一人で行った」という動機づけ**(「狙われてるのは、たぶん茜音のほうだ」等)**も本文に出さない**(削除済み)
- **背景にある全話共通ルール(確定・重要): 茜音を「小言を言うキャラ」にしない。** 咎める・約束で縛る・皮肉を言う・不機嫌で圧をかける、はすべて禁止。心配は責める形ではなく「知りたい」という前のめりさとして出す。詳細は`bible/characters/akane.md`「茜音を『小言を言うキャラ』にしない」の節を必読
- 冬夜が抱えている宿題: **叔父・浩二に連絡すると決めたが、まだ送っていない**(第26話で送る)

## 直近3話の分量(4,000字前後が目安)

第23話3,937字 / 第24話4,149字 / 第25話3,995字

## 参照先(重複を避けるためポインタのみ)

- 未回収の伏線一覧・状態は`plot/foreshadowing.md`
- 文体・分量・冗長回避のルールは`CLAUDE.md`
- 各話の詳細は`bible/timeline.md`のイベント年表
